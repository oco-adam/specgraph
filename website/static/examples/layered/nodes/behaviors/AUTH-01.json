{
  "$schema": "https://oco-adam.github.io/specgraph/schemas/node.schema.json",
  "id": "AUTH-01",
  "type": "behavior",
  "title": "Successful Login",
  "expectation": "When a user submits valid credentials, the system creates a session and redirects to the dashboard.",
  "constraints": ["A secure session cookie must be issued before redirect."],
  "verification": "npm test -- --grep AUTH-01"
}
