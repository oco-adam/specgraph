{
  "$schema": "https://oco-adam.github.io/specgraph/schemas/node.schema.json",
  "id": "AUTH-04",
  "type": "behavior",
  "title": "Successful Login Redirect",
  "expectation": "After submitting valid credentials, user is redirected to the dashboard page",
  "constraints": ["Session token must be stored before redirect"],
  "verification": "npm test -- --grep AUTH-04",
  "links": {
    "depends_on": [
      "AUTH-01"
    ],
    "implements": [
      "DOM-USER-01"
    ]
  }
}
