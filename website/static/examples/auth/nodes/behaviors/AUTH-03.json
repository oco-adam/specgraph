{
  "$schema": "https://oco-adam.github.io/specgraph/schemas/node.schema.json",
  "id": "AUTH-03",
  "type": "behavior",
  "title": "Password Validation Error",
  "expectation": "When user submits a password shorter than 8 characters, an inline error message appears below the password field",
  "constraints": ["Error message must appear without page reload"],
  "verification": "npm test -- --grep AUTH-03",
  "links": {
    "depends_on": [
      "AUTH-01"
    ]
  }
}
