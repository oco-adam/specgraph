{
  "$schema": "https://oco-adam.github.io/specgraph/schemas/node.schema.json",
  "id": "AUTH-05",
  "type": "behavior",
  "title": "Session Persistence",
  "expectation": "When user refreshes the page after login, they remain authenticated and are not redirected to the login page",
  "invariant": "Session must survive page reload",
  "verification": "npm test -- --grep AUTH-05",
  "status": "approved",
  "links": {
    "depends_on": ["AUTH-04"],
    "implements": ["DOM-USER-01"]
  }
}
